# @package _global_
defaults:
  - _self_
  - extraction: name
  - embedding: fastText
  - local: docker
  - dataset: gitranking

  - override hydra/hydra_logging: colorlog
  - override hydra/job_logging: colorlog


num_workers: 4