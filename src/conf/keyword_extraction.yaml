# @package _global_
defaults:
  - _self_
  - content: name
  - embedding: SciBERT
  - local: default
  - dataset: gitranking
  - keyword: yake

  - override hydra/hydra_logging: colorlog
  - override hydra/job_logging: colorlog


num_workers: 1

annotations_path: ${base_path}/data/processed/annotations/

force_new: False
seed: 42

project_path: ${base_path}/data/raw/dataset_with_graphs.csv
keywords_out: ${base_path}/data/processed/keywords